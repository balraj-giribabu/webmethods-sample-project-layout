pipeline{
	agent any
	environment{
		var_package_name = "${PACKAGE_TO_DEFINE}"
	}
	
	stages{
		stage('printing'){
			steps{
				script{
					for(item in var_package_name){
					    println item
					}
				}
			}
		}
	}
}
