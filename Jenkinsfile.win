pipeline{
	agent any
	environment{
		random_packages = "${PACKAGE-LIST}"
	}
	
	stages{
		stage("Creating Loop"){
			steps{
				script{
					for (pack in random_packages.split(',')){
						println pack
						bat """
						dir
						dir
						"""
					}
				}
			}
		}
	}
}
